const array = Array.from({ length: 10 }, () => Math.floor(Math.random() * 100) + 1)

array

function mergeSort(numbers) {
    // split array down until its in arrays of length 2
    // for each array put them in ascending order
    // arrange arrays in size order
    // put
}
